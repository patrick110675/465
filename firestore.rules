rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 目前平台尚未要求登入，先開放讀寫供內部測試。
    // 正式公開前應改成管理員驗證規則。
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
